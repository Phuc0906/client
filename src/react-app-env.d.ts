/// <reference types="react-scripts" />
type FormValues = {
    fullname?: string;
    email: string;
    password: string;
};
interface Coordinate {
    bottom?: number;
    left?: number;
    width: number;
}
interface userInputProp {
    className?: string;
}
